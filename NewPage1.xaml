<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LAB2_OOP_MAUI.NewPage1"
             Title="Пошук Секцій"
             BackgroundColor="#f0f2f5">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="20">

            <Label Text="Спорт на факультеті" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" 
                   TextColor="#333"/>

            <Frame BorderColor="Gray" CornerRadius="10" Padding="15" BackgroundColor="White">
                <VerticalStackLayout Spacing="5">
                    <Label Text="Оберіть метод аналізу:" FontAttributes="Bold" TextColor="Black"/>

                    <RadioButton x:Name="RbLinq" Content="LINQ to XML" IsChecked="True" GroupName="StrategyGroup"/>
                    <RadioButton x:Name="RbDom" Content="DOM API" GroupName="StrategyGroup"/>
                    <RadioButton x:Name="RbSax" Content="SAX API" GroupName="StrategyGroup"/>
                </VerticalStackLayout>
            </Frame>

            <Frame BorderColor="Gray" CornerRadius="10" Padding="15" BackgroundColor="White">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Критерії пошуку:" FontAttributes="Bold" TextColor="Black"/>

                    <Entry x:Name="NameEntry" Placeholder="Назва секції (напр. Basketball)" />
                    <Entry x:Name="CoachEntry" Placeholder="Прізвище тренера" />
                    <Entry x:Name="TimeEntry" Placeholder="Час занять" />

                    <HorizontalStackLayout Spacing="10" HorizontalOptions="Center" Margin="0,10,0,0">
                        <Button Text="🔍 Пошук" 
                                Clicked="OnSearchClicked" 
                                BackgroundColor="#42b72a" 
                                TextColor="White"
                                WidthRequest="120"/>

                        <Button Text="🧹 Clear" 
                                Clicked="OnClearClicked" 
                                BackgroundColor="#e4e6eb" 
                                TextColor="Black"
                                WidthRequest="100"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <VerticalStackLayout Spacing="5">
                <Label Text="Результати:" FontAttributes="Bold" FontSize="18" TextColor="#333"/>
                <Editor x:Name="ResultsEditor" 
                        HeightRequest="200" 
                        IsReadOnly="True" 
                        BackgroundColor="White"
                        Placeholder="Тут з'являться результати..." />
            </VerticalStackLayout>

            <Button Text="Вихід з програми" 
                    Clicked="OnExitClicked" 
                    BackgroundColor="#ff4444" 
                    TextColor="White" 
                    Margin="0,20,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>